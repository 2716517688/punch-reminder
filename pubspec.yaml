name: punch_reminder
description: 下班打卡提醒 - 离开公司自动提醒打开纷享销客
publish_to: 'none'
version: 1.2.1+5

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^13.0.0
  flutter_local_notifications: ^18.0.0
  shared_preferences: ^2.2.0
  permission_handler: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
